# 🔄 RAG Workflow: Step-by-Step Visual Guide

## 📊 Complete Workflow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          STEP 1: SETUP                               │
│                                                                      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐     │
│  │ Install  │ →  │   Get    │ →  │ Configure│ →  │  Ready!  │     │
│  │  Deps    │    │ API Keys │    │   .env   │    │          │     │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘     │
│       ↓               ↓               ↓                ↓            │
│  pip install   Gemini + Pinecone  Edit .env     All set!          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    STEP 2: CREATE EMBEDDINGS                         │
│                      (This is "training")                            │
│                                                                      │
│  ┌────────────┐       ┌──────────────┐       ┌─────────────┐      │
│  │   Load     │   →   │   Convert    │   →   │   Create    │      │
│  │ dataset.csv│       │   to Text    │       │  Embeddings │      │
│  └────────────┘       └──────────────┘       └─────────────┘      │
│  15,000 records       Rich descriptions       384D vectors         │
│                                                                      │
│                       ┌──────────────┐                              │
│                   →   │    Store     │                              │
│                       │   in VectorDB│                              │
│                       └──────────────┘                              │
│                       ChromaDB or Pinecone                          │
│                                                                      │
│  Command: python train_rag_embeddings.py                           │
│  Time: 1-3 minutes                                                  │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      STEP 3: START SERVER                            │
│                                                                      │
│  ┌────────────┐       ┌──────────────┐       ┌─────────────┐      │
│  │   Start    │   →   │   Server     │   →   │   Ready     │      │
│  │   Server   │       │   Running    │       │ for Queries │      │
│  └────────────┘       └──────────────┘       └─────────────┘      │
│  python                localhost:8001         API endpoints         │
│  rag_server.py         FastAPI docs           /query /search        │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      STEP 4: QUERY & USE                             │
│                                                                      │
│  ┌────────────┐       ┌──────────────┐       ┌─────────────┐      │
│  │   User     │   →   │   Retrieve   │   →   │    LLM      │      │
│  │  Question  │       │   Similar    │       │  Generates  │      │
│  └────────────┘       └──────────────┘       └─────────────┘      │
│  "What is avg         Top 5 relevant          Data-driven          │
│   income?"            profiles                answer                │
│                                                                      │
│                       ┌──────────────┐                              │
│                   →   │   Return     │                              │
│                       │   Answer +   │                              │
│                       │   Sources    │                              │
│                       └──────────────┘                              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Two Paths: ChromaDB vs Pinecone

### Path A: ChromaDB (Local)

```
┌─────────────────────────────────────────┐
│           CHROMADB WORKFLOW              │
├─────────────────────────────────────────┤
│                                          │
│  1. pip install -r requirements-rag.txt │
│     ↓                                    │
│  2. python train_rag_embeddings.py      │
│     ↓                                    │
│  3. Data stored in: ml/vector_db/       │
│     ↓                                    │
│  4. python rag_server.py                │
│     ↓                                    │
│  5. Query at: localhost:8001/docs       │
│                                          │
│  ✅ FREE                                 │
│  ✅ Fast setup (5 min)                   │
│  ✅ Works offline                        │
│  ✅ Perfect for development              │
│                                          │
│  ❌ Local storage only                   │
│  ❌ Single machine                       │
└─────────────────────────────────────────┘
```

### Path B: Pinecone (Cloud)

```
┌─────────────────────────────────────────┐
│           PINECONE WORKFLOW              │
├─────────────────────────────────────────┤
│                                          │
│  1. Sign up: pinecone.io                │
│     ↓                                    │
│  2. Create index (384 dimensions)       │
│     ↓                                    │
│  3. Get API key                          │
│     ↓                                    │
│  4. Configure .env                       │
│     ↓                                    │
│  5. pip install -r requirements-        │
│     pinecone.txt                         │
│     ↓                                    │
│  6. python rag_pipeline_pinecone.py     │
│     ↓                                    │
│  7. Data stored in: Pinecone Cloud      │
│     ↓                                    │
│  8. python rag_server.py                │
│     ↓                                    │
│  9. Query at: localhost:8001/docs       │
│                                          │
│  ✅ Cloud-hosted                         │
│  ✅ Scalable                             │
│  ✅ Auto backups                         │
│  ✅ Perfect for production               │
│                                          │
│  ❌ Requires internet                    │
│  ❌ Paid (free tier available)           │
└─────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Traditional ML Flow (Existing)

```
┌────────────┐
│   Input    │ Age: 35, Income: $75k
└─────┬──────┘
      │
      ▼
┌────────────┐
│  XGBoost   │ Trained model
│   Model    │
└─────┬──────┘
      │
      ▼
┌────────────┐
│   Output   │ Risk Score: 65
└────────────┘

Time: Milliseconds
Purpose: Predictions
```

### RAG Flow (New)

```
┌────────────┐
│   Query    │ "What's typical for 35yo with $75k income?"
└─────┬──────┘
      │
      ▼
┌────────────┐
│  Embedding │ Convert query to vector
└─────┬──────┘
      │
      ▼
┌────────────┐
│  Search    │ Find similar profiles in vector DB
│  VectorDB  │
└─────┬──────┘
      │
      ▼
┌────────────┐
│  Retrieve  │ Top 5 relevant profiles
└─────┬──────┘
      │
      ▼
┌────────────┐
│    LLM     │ Generate answer using context
│  (Gemini)  │
└─────┬──────┘
      │
      ▼
┌────────────┐
│   Answer   │ "Based on 237 similar profiles,
│  + Sources │  average risk score is 67..."
└────────────┘

Time: 2-5 seconds
Purpose: Insights & Explanations
```

---

## 🎨 Frontend Integration Flow

```
┌───────────────────────────────────────────────────────────────┐
│                     Next.js Application                        │
│                                                                │
│  ┌──────────────┐         ┌──────────────┐                   │
│  │  Dashboard   │         │   AI Chat    │                   │
│  │              │         │              │                   │
│  │  Show ML     │         │  Ask RAG     │                   │
│  │  predictions │         │  questions   │                   │
│  └──────┬───────┘         └──────┬───────┘                   │
│         │                        │                            │
│         ▼                        ▼                            │
│  ┌─────────────────────────────────────────┐                 │
│  │         src/lib/rag-api.ts              │                 │
│  │  TypeScript client for RAG endpoints    │                 │
│  └──────────────┬──────────────────────────┘                 │
│                 │                                             │
└─────────────────┼─────────────────────────────────────────────┘
                  │
                  │ HTTP Requests
                  │
┌─────────────────▼─────────────────────────────────────────────┐
│                    ML + RAG Servers                            │
│                                                                │
│  ┌──────────────────┐         ┌──────────────────┐           │
│  │   ML Server      │         │   RAG Server     │           │
│  │   Port 8000      │         │   Port 8001      │           │
│  │                  │         │                  │           │
│  │  /predict/risk   │         │  /query          │           │
│  │  /predict/health │         │  /search         │           │
│  └────────┬─────────┘         └────────┬─────────┘           │
│           │                            │                      │
│           ▼                            ▼                      │
│  ┌──────────────┐         ┌────────────────────┐            │
│  │  XGBoost     │         │  Vector Database   │            │
│  │  Models      │         │  (Chroma/Pinecone) │            │
│  └──────────────┘         └────────────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 Example: Complete User Flow

### Scenario: User Asks Financial Question

```
1. User Types in AI Chat
   ┌────────────────────────────────────┐
   │ "Should I invest in stocks?"       │
   └────────────────────────────────────┘
                ↓

2. Frontend Calls RAG API
   ┌────────────────────────────────────┐
   │ ragAPI.query(question)             │
   └────────────────────────────────────┘
                ↓

3. RAG Server Processes
   ┌────────────────────────────────────┐
   │ a) Convert question to embedding   │
   │ b) Search vector DB                │
   │ c) Find 5 similar profiles         │
   │ d) Send to Gemini with context     │
   │ e) Generate answer                 │
   └────────────────────────────────────┘
                ↓

4. Return Answer + Sources
   ┌────────────────────────────────────┐
   │ "Based on 127 profiles like yours: │
   │ - Age 30-40, Income $70k-$90k      │
   │ - 73% chose moderate risk stocks   │
   │ - Average return: 8.5%             │
   │                                    │
   │ Recommendation: Start with         │
   │ 60% stocks, 40% bonds..."          │
   │                                    │
   │ Sources:                           │
   │ - Profile #234 (35yo, $75k)        │
   │ - Profile #891 (32yo, $82k)        │
   │ - Profile #1203 (38yo, $78k)       │
   └────────────────────────────────────┘
                ↓

5. Display to User
   ┌────────────────────────────────────┐
   │ User sees AI-generated answer      │
   │ backed by real data from dataset   │
   └────────────────────────────────────┘
```

---

## ⚡ Quick Command Reference

### Initial Setup

```powershell
# ChromaDB (Local)
cd ml
pip install -r requirements-rag.txt
copy .env.example .env
notepad .env  # Add GEMINI_API_KEY
python train_rag_embeddings.py
python rag_server.py

# Pinecone (Cloud)
pip install -r requirements-pinecone.txt
# Add PINECONE_API_KEY to .env
python rag_pipeline_pinecone.py
python rag_server.py
```

### Daily Development

```powershell
# Start servers
python rag_server.py  # RAG on port 8001
python server.py      # ML on port 8000

# Test
curl http://localhost:8001/health
```

### Update Data

```powershell
# After adding to dataset.csv
python train_rag_embeddings.py --force-rebuild
```

---

## 📊 Performance Comparison

```
┌────────────────────┬─────────────┬──────────────┐
│     Operation      │    Time     │   When to Use│
├────────────────────┼─────────────┼──────────────┤
│ ML Prediction      │  50-200ms   │ Fast results │
│ RAG Retrieval      │  50-200ms   │ Find data    │
│ RAG Full Query     │  2-5s       │ Explanations │
│ Create Embeddings  │  1-3 min    │ First setup  │
│ Update Embeddings  │  1-3 min    │ New data     │
└────────────────────┴─────────────┴──────────────┘
```

---

## 🎯 When to Use What

### Use Traditional ML When:

- ✅ Need fast predictions (< 1 second)
- ✅ Simple numeric output (risk score, health score)
- ✅ No explanation needed
- ✅ High-frequency requests

### Use RAG When:

- ✅ Need explanations ("why this score?")
- ✅ User asks questions in natural language
- ✅ Want to show similar examples
- ✅ Need contextual insights

### Use Both When:

- ✅ Show prediction + explanation
- ✅ Prediction with data-backed reasoning
- ✅ Interactive financial advisor
- ✅ Best user experience! 🚀

---

## 🎉 Success Checklist

After setup, you should be able to:

- [ ] Visit http://localhost:8001/docs
- [ ] See API documentation
- [ ] POST to `/query` endpoint
- [ ] Get answer with sources
- [ ] Search by criteria
- [ ] Get statistics
- [ ] Query from Next.js app
- [ ] See results in frontend

---

## 📚 Where to Go Next

1. **Just starting?** → Read `QUICKSTART.md`
2. **Want to understand?** → Read `TRAINING_GUIDE.md`
3. **Need all details?** → Read `RAG_GUIDE.md`
4. **Going to production?** → Read `PINECONE_GUIDE.md`
5. **Stuck?** → Check error messages, review guides

---

**Visual guides complete!** 🎨

All files are ready in your `ml/` directory. Start with `QUICKSTART.md`!
